Ghost-FWL/**
ghost_datasets/**
.venv/**
__pycache__/
*.pt
# data caches (regenerable from the dataset)
runs/**/*.npz
runs/*cache*/
# verbose run logs (keep the json/png results, drop the logs)
*.log
# model checkpoints + rerun recordings (large, regenerable)
*.pth
*.rrd
# retrain/viz outputs (checkpoints, recordings — regenerable from the cached reps)
downstream/outputs/retrain/
downstream/outputs/vis/
# machine-local path config (use env.yaml.example as the template)
env.yaml
# local editor / Claude settings, large binaries
.claude/
.mcp.json
*.pdf
examples.png
